<div id="title" class="title">
  <h1>Reviewers</h1>
  <br><br>
</div>

<div id="user">
  <% @users.each do |user| %>
    <p class="lead"><%= link_to user.name, user_reviews_path(user) %><br><%= user.reviews.count %> Reviews</p>
  <% end %>
</div>
